/*function toggleVideo(video, state) {

    var div = document.getElementById(video);
    var iframe = div.getElementsByTagName("iframe")[0].contentWindow;
    //div.style.display = state == 'hide' ? 'none' : '';

    func = '';
    switch(state){
    	case 'play':
    	alert(video+' - '+state);
    		func = 'playVideo';
    		break;
    	case 'hide':
    		func = 'pauseVideo';
    		break;
    }
    iframe.postMessage('{"event":"command","func":"' + func + '","args":""}','*');
}*/

jQuery(function($){
	$('.positive-fitvids').fitVids().each(function(i){
		$(this).find('> div').attr('id','video'+i);
	})
	$('.positive-fitvids').find('> img').click(function(){
		$(this).fadeOut(600);
		var videoid = $(this).parent().find('>div').attr('id');
		//toggleVideo(videoid, 'play');
        alert(videoid);
        div = document.getElementById(videoid);
        iframe = div.getElementsByTagName("iframe")[0].contentWindow;
        //iframe.postMessage('{"event":"command","func":"playVideo","args":""}','*');
        iframe.click(function(){ alert('click!')})
	});
});